#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int main(int argc, char **argv) {
        if (argc < 2) {
                printf("kot: no input files\n");
                return -1;
        }
        else {
                return 0;
        }

        return 0;
}
